# NoobAI XL V-Pred 1.0 - Python Dependencies
# Tested on Ubuntu/WSL and Windows with Python 3.11/3.12
# Install with: pip install -r requirements.txt

# ============================================================================
# PYTORCH - Install FIRST, separately (platform-specific)
# ============================================================================
# NVIDIA RTX 50xx (Blackwell) - CUDA 12.8 (requires PyTorch 2.7+ for sm_120 support):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# NVIDIA RTX 30xx/40xx (Ampere/Ada) - CUDA 12.4:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# NVIDIA RTX 20xx (Turing) - CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Linux/WSL CPU Only:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# macOS (Apple Silicon):
#   pip install torch torchvision torchaudio
#
# Then run: pip install -r requirements.txt
# ============================================================================

# Core ML Framework
# 2.7+ required for Blackwell (sm_120) native support and CUDA 12.8 compute capability
torch>=2.7.0,<3.0.0

# Diffusion Models and Pipeline
diffusers>=0.30.0,<0.40.0
transformers>=4.40.0,<5.0.0

# CPU Offloading for Low-VRAM GPUs
# Required for sequential CPU offloading (enables inference on 6GB GPUs)
accelerate>=0.21.0,<2.0.0

# DoRA Adapter Support
# Required for loading DoRA (Weight-Decomposed Low-Rank Adaptation) adapters
peft>=0.12.0,<1.0.0

# Long Prompt Support
# Extends beyond 77-token CLIP limit with A1111 weight syntax support (text:1.2)
# Install from GitHub: pip install git+https://github.com/xhinker/sd_embed.git@main
sd_embed @ git+https://github.com/xhinker/sd_embed.git@main

# Web Interface
gradio>=4.0.0,<7.0.0

# Image Processing
pillow>=10.0.0,<12.0.0

# Model format optimization
safetensors>=0.4.0,<1.0.0

# Data processing for CSV search functionality (character/artist autocomplete)
pandas>=2.0.0,<3.0.0

# Additional dependencies (auto-installed but specified for clarity)
huggingface-hub>=0.20.0,<2.0.0
numpy>=1.24.0,<3.0.0

# Pydantic constraint - conservative upper bound, bug status in newer gradio untested
pydantic>=2.0.0,<2.11.0

# ============================================================================
# OPTIONAL PERFORMANCE PACKAGES
# ============================================================================
#
# SageAttention - ~5x attention speedup on RTX 5090 (Blackwell sm_120)
# Uses FP4 microscaling on Blackwell tensor cores. Incompatible with
# torch.compile (--optimize flag) — choose one or the other.
# Requires CUDA 12.8 + PyTorch 2.7+. Install separately:
#   pip install sageattention
# sageattention>=2.0.0  # uncomment if desired

# ============================================================================
# VERSION CONSTRAINTS (IMPORTANT - DO NOT REMOVE)
# ============================================================================
# Upper bounds prevent breaking changes from newer package versions:
#
# - torch>=2.7.0: Minimum for Blackwell (sm_120) native CUDA 12.8 support
# - diffusers<0.40.0: Tested up to 0.37.0; watch for decode() API changes above 0.40
# - accelerate<2.0.0: Tested up to 1.13.0; watch for 2.0 API restructuring
# - huggingface-hub<2.0.0: Tested up to 1.x; required by diffusers 0.37+
# - numpy<3.0.0: PyTorch 2.3.1+ is compatible with numpy 2.x
# - gradio<7.0.0: Tested up to 6.9.0
# - pydantic<2.11.0: Bug status with gradio 6.x untested; conservative bound maintained
#
# These constraints have been tested for stability. To upgrade to newer
# versions, test thoroughly in a separate environment first.
# ============================================================================

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
#
# 1. Install Python 3.11 or 3.12 (NOT 3.13 - too new for ML packages)
#    Download from: https://www.python.org/downloads/
#
# 2. Install PyTorch with appropriate CUDA version for your GPU:
#
#    RTX 5090 (Blackwell) - requires PyTorch 2.7+ for sm_120 native support:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
#    RTX 30xx/40xx (Ampere/Ada):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
#    RTX 20xx (Turing):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 3. Install remaining dependencies:
#    pip install -r requirements.txt
#
# 4. Verify installation:
#    python -c "import torch; print(f'PyTorch {torch.__version__}, CUDA: {torch.cuda.is_available()}')"
#
# WINDOWS USERS: If you encounter "ModuleNotFoundError: No module named 'transformers.utils'"
# after installation, this indicates a corrupted transformers installation. Fix with:
#    pip uninstall transformers -y && pip install "transformers>=4.40.0,<5.0.0"
#
# ============================================================================
# TROUBLESHOOTING
# ============================================================================
#
# Windows Long Path Issues:
#   If you get "path too long" errors, enable long paths:
#   1. Run PowerShell as Administrator
#   2. Run: New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
#   3. Restart computer
#
# CUDA Not Detected:
#   - Ensure NVIDIA drivers are installed (545+ for RTX 5090)
#   - Verify with: nvidia-smi
#   - Reinstall PyTorch with correct CUDA version for your GPU
#
# Package Conflicts:
#   - Use Python 3.11 or 3.12 (avoid 3.13)
#   - Fresh virtual environment recommended:
#     python -m venv venv
#     venv\Scripts\activate  (Windows)
#     source venv/bin/activate  (Linux/Mac)
#
# Corrupted Transformers Installation (Windows):
#   If you see "ModuleNotFoundError: No module named 'transformers.utils'":
#   - This is caused by pip cache corruption on Windows
#   - Fix: pip uninstall transformers -y && pip install "transformers>=4.40.0,<5.0.0"
#   - Alternative: pip cache purge && pip install -r requirements.txt
#
# ============================================================================
